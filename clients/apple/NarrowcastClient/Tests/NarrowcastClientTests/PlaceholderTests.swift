import XCTest
@testable import NarrowcastClient

// Real client tests live alongside an integration harness (running narrowcast
// in --simulate mode). Placeholder for now.
final class PlaceholderTests: XCTestCase {
    func testModuleLinks() {
        XCTAssertTrue(true)
    }
}
