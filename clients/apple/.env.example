# Copy to .env and fill in values from App Store Connect →
# Users and Access → Integrations → Team Keys.
# .env is gitignored so the IDs stay out of version control.

ASC_KEY_ID=YOUR_KEY_ID
ASC_ISSUER_ID=YOUR_ISSUER_ID
